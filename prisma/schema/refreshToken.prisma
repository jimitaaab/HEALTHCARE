model RefreshToken {
  id        String   @id @default(cuid())
  token     String
  userId    String
  expiresAt DateTime

  user User @relation(fields: [userId], references: [id])

  @@map("refresh_tokens")
}